{
  "name": "contactum",
  "private": true,
  "scripts": {
    "dev": "concurrently -r -k \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
